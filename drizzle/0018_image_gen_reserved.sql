-- Резерв квоты на время генерации.
--
-- Проверка остатка и запись результата разнесены на десятки секунд: между
-- ними идёт запрос к модели. Без резерва десять параллельных запросов видят
-- один и тот же остаток и проходят все, а платит владелец площадки.
--
-- Счётчик увеличивается одним UPDATE с условием — это атомарно на уровне
-- строки и не держит блокировку на время генерации.
ALTER TABLE "users" ADD COLUMN "image_gen_reserved" integer DEFAULT 0 NOT NULL;
