/**
 * Требуемое разрешение картинки баннера. Проверку размеров делает браузер при
 * загрузке — здесь эти числа живут ради Swagger: клиент и админ должны видеть
 * одно и то же требование, а не два расходящихся числа в двух репозиториях.
 *
 * Меняя их, поменяйте и `BANNER_IMAGE` в клиенте (`lib/api/banners.ts`).
 */
export const BANNER_IMAGE_WIDTH = 1240;
export const BANNER_IMAGE_HEIGHT = 400;

/** Сколько баннеров отдаём на витрину: карусель длиннее никто не долистывает. */
export const MAX_ACTIVE_BANNERS = 10;
