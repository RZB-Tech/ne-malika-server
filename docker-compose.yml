services:
  postgres:
    image: postgres:18
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nemalika
    ports:
      - '5450:5432'
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
